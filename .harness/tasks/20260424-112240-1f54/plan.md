# Plan

1. 明确 把 Codex 和 Claude Code 的 sub-agent 生成情况补进试用/设计文档
然后再决定是否让宿主主路径优先走 Planner/Evaluator sub-agent，而不是只停留在“定义文件已生成”阶段。
 对应的修改边界：相关实现路径
2. 在最小改动范围内完成实现或修复
3. 按验收标准自检：目标“把 Codex 和 Claude Code 的 sub-agent 生成情况补进试用/设计文档
然后再决定是否让宿主主路径优先走 Planner/Evaluator sub-agent，而不是只停留在“定义文件已生成”阶段。
”已被实现或修复；变更范围与目标一致；输出结果可供后续 verify 使用
